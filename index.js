import React from 'react';
import ReactDOM from 'react-dom/client';
import HarfGame from './App';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <HarfGame />
  </React.StrictMode>
);
